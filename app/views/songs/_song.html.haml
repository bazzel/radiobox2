.pure-u-1-3.photo-box{ data: { channel: song.channel } }
  = image_tag asset_path("#{song.channel}.png")
  %aside.photo-box-caption
    .title{ data: { attribute: 'title' }}= song.title
    %span{ data: { attribute: 'artist' }}= song.artist
